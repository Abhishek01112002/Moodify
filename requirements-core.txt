# Core runtime dependencies for Spotify Moodify app
# Tested with Python 3.11+ - all have pre-built wheels
pandas>=2.2.0,<3.0
numpy>=2.0.0,<3.0
scikit-learn>=1.5.0,<2.0
plotly>=5.24.0,<6.0
seaborn>=0.13.0,<1.0
matplotlib>=3.9.0,<4.0
tqdm>=4.66.3,<5.0
python-dotenv>=1.0.0,<2.0
spotipy>=2.24.0,<3.0
streamlit>=1.38.0,<2.0
rapidfuzz>=3.9.0,<4.0
faiss-cpu>=1.10.0,<2.0
fastapi>=0.115.0,<1.0
uvicorn>=0.30.0,<1.0

# Security baseline constraints for transitive dependencies
requests>=2.32.2,<3.0
urllib3>=2.2.2,<3.0
tornado>=6.4.2,<7.0
jinja2>=3.1.6,<4.0

# Note: pyarrow requires compilation—install separately if needed:
# py -3.13 -m pip install pyarrow --prefer-binary

# Optional heavy dependencies (install separately as needed)
# py -3.13 -m pip install tensorflow tensorflow-recommenders mlflow
# py -3.13 -m pip install "dvc[all]" kaggle polars
